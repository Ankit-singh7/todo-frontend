import { SortCountriesPipe } from './sort-countries.pipe';

describe('SortCountriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCountriesPipe();
    expect(pipe).toBeTruthy();
  });
});
